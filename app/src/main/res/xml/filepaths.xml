<?xml version="1.0" encoding="utf-8"?>
<paths>
    <files-path path="./" name="share" />
    <cache-path name="cache" path="." />
    <!-- Optionally, add other paths like external storage if necessary -->
    <external-path name="external_files" path="." />
</paths>